# Changelog

Draft-stage repo: versions move fast pre-1.0, and process docs may run ahead of the engine **only** when the debt is marked in the gate table and recorded as an open obligation in `docs/GAPS.md` (the layer-parity rule, v0.5.3). Run `./install.sh --check` in a consuming repo to see what an upgrade changes. Tags land only at doc/engine parity milestones. **The top entry of this file is the package version — the single authority (v0.5.5, pilot N2).** File headers carry the version of the commit that last modified them — checked against git history (`git log -1 -- <file>`), not merely against the entry list, or every stale stamp passes (pilot D2); `formats.json`'s stamp must equal the top entry. Both checks join the parity sweep (OBL-PKG-08).

## v0.7.3-draft — 2026-09-19 · **RELEASED 2026-09-20** (tag `v0.7.3-draft`; first release to ship only after two fresh-context reviews of the built round plus a delta review of the fixes came back fully remediated)
- **Pilot #3 (cloakid-validation-prodsim) onboarded and filed the first fully-current batch ever received** — 14 findings (prodsim/F-60…F-76), every one against live HEAD, zero triage waste — plus platform F-65…F-70 filed from the v0.6.1→v0.7.2 upgrade itself. The intake-coupling policy held: platform upgraded before filing; prodsim installed current. Frisbii dormant at v0.5.0; the upgrade-RCA question remains unanswered in both older pilots. The whole round was **adversarially reviewed before implementation** (16 findings against the proposal; 3 confirmed defects in the original draft corrected — the ev:commit arm would have turned the suite red and re-created platform/F-65's permanently-red shape in real repos, the escrow widening would have undone F-36 and F-51 at once, and the doc-reduction would have relocated four live rules into files consumers never receive). Verification honesty: fixes proven against fixtures and engine-side reproductions.
- **A mechanism states its subject** (prodsim F-70.2/F-73.1/F-74.3/F-75.3/F-76.4 — five independent arrivals at one demand, adopted once, generically; class closure, vacuity family): every gate with a `subject` registry label reports its count in the PASS output and a green over zero subjects says VACUOUS ('6/6 passed' over an empty set survived two wave boundaries as a recorded check); status.mjs names the branch it derived from and warns when a live run's checkout is not a run branch (prodsim/F-73 — the same command on two branches gave a resuming agent confident opposite answers); the audit-trigger deriver reports rows-seen/rows-parsed instead of 'not recorded' about a recorded table (prodsim/F-74).
- **ev:commit citations RESOLVE — OBL-PKG-23 discharged** (prodsim/F-68; instance fix + the checkable sub-case of the citation-truth class): blocking in the run tree and at pre-commit for staged run files (four well-formed false citations shipped in one run; the habit is writing the citation before the commit exists, and the message names the remedy), ADVISORY in durable docs (ADV-2: dangling shas there are legitimate history — branch GC, shallow clones, rewrites). The arm's first catch was itself: the gates' rejection log quotes failing citations verbatim and is now scan-excluded.
- **The escrow reads the files CV records are born in** (prodsim/F-69, ADV-3 — instance fix, escrow family): FORMATS §5 has the verifier allocate the record in reports/U<n>-verify.md and the escrow read only RUN-REPORT — three genuine coverage limits reached no durable home while the escrow reported clean; verify reports join the per-run aggregation with F-36's mid-flight shield and F-51's cross-file discharge intact. P3's verifier brief is imperative (allocate the CV, cite its full id) and verifiers run gate-3 over their own report before handback.
- **The freeze, the base, and the header stop refusing correct repos** (parser/config family, three instance fixes): `legacy_freeze_exclude` carves live runtime paths out of GATE-11 (prodsim/F-60 — a pilot forwent the gate entirely against a demonstrated risk); `run_base` + `gates.sh check-id --base` refuse a run base that does not carry the plan (prodsim/F-65 — 'from main' produced a base 258 commits stale on which the run did not exist); GATE-8's F-28 scan starts at the first `##` heading (platform/F-70 — a signed plan failed its own gate unsatisfiably on its own schema header fields).
- **The mask, the counter, and the resolver stop lying** (deriver family): the doc-wide backtick mask may not pair across table rows (prodsim/F-72 — one stray tick blanked the next row's citations and GATE-3 reported real evidence missing); status counting is row-scoped plus sanctioned `status:` sentences, outside fences and code spans (prodsim/F-75, ADV-4 — writing ABOUT a status changed the count of it, and the honesty-contract amendment pattern inflated the metric it was amending); GATE-9 at G4 resolves exactly the reconciled v<N+1>, blocked drafts skipped, refusal naming the --spec route (prodsim/F-76, ADV-5 — resolver and playbook disagreed by one version for every G4 by construction).
- **GATE-13 refuses credential disclosure** (platform/F-69 — narrow class closure, shipped twice in one repo): a Verify default-expanding a credential-named variable into echo/printf prints the value exactly when the residue probe fires; safe forms named, `lint-ok:` stays the escape, no backtick exemption (ADV-10).
- **Finding ids qualify on crossing** (prodsim/F-67, PO decision): `F-nn` stays repo-local; any citation outside the allocating repo — including the installed package files — is written qualified (`platform/F-58`), the four live collisions are requalified, older bare history is grandfathered (FORMATS §13). Arithmetic across repos is a race the pilot proved lost on its first try.
- **Docs that close improvisation routes**: registry identifiers go in inline backticks and a backticked FIRST cell is not a row (prodsim/F-71); P2's resume-before-replan entry condition (platform/F-68 — seven review rounds re-derived a signed, unexecuted unit); P4 reconciles VF-* findings into SPEC v<N+1> like DEV-* (platform/F-67 — the only door to the next planner); P4 carries a literal audit-trigger example row; INSTALL states the CLAUDE.md section is replaced whole with --check showing the first differing lines (platform/F-66), documents the Jira hierarchy constraint (prodsim/F-61 — Story and Task are siblings at level 0), and the router template drops its mapping restatement (ADV-11 — it was the drift home F-66 documented); the feedback log is per-branch until merged (prodsim/F-73.4); P5 states archival is a phase step, not an escape hatch.
- DECLINED, with reasons: registry-format migration (prodsim/F-71.3 — every consumer parses the table; the doc rule + mask fix remove the observed hazard); cross-branch status derivation (prodsim/F-73.3 — large; the silence is removed instead); mangled-identifier heuristics (prodsim/F-71.2 — cry-wolf risk, the F-45 lesson); AT-3 metric widening (prodsim/F-74.4 — a semantics change deserving more than one run's data). OBL-PKG-25 registered: the v0.7.4 context-reduction round, finding-trace first, per the adversarial review's headline condition.
- Suite 305→331 assertions; 12 mutations (M21–M32), each killed by exactly its own tests — M32 needed two attempts to write a mutation the tests could see, which is the proof working.
- **R9: two fresh-context reviews of this round before publish, then full remediation** (PO decision 2026-09-20; both reports in docs/reviews/ — adversarial REVIEW-v073-adversarial-ADV-R9.md, 11 CONFIRMED, and developer-persona REVIEW-v073-developer-DEV-R9.md, 16 findings; both verdicts were "not as-is", and every blocking finding landed in this same unpublished round):
  - **The escrow satisfies itself only on claim text** (ADV-R9-01, high): discharge matching masks fences and inline code — a fenced "example of the record format" carrying a fabricated ev: discharged a real CV; discovery stays on raw text, the same mention-vs-claim asymmetry gate-3 has. Discharge is order-independent across the per-run file set (ADV-R9-04) and one verdict per id.
  - **The deriver's cell split matches the engine's** (ADV-R9-02, high): status.mjs ports `_cells` (unescaped-pipe split, last cell kept without a trailing pipe) — two BLOCKED rows counted as one and AT-3 reported a real hit green on legal GFM. GAPS row parsing reads through the same helper.
  - **The ev:commit resolver tracks fences** (ADV-R9-03, high): quoting the gate's own refusal — which the F-43 paste rule mandates — used to block the quoting commit forever; the runs/.gate-log exclusion was this same defect patched one file wide.
  - **The F-28 exemption is header-shaped, not positional** (ADV-R9-05): normative PROSE above the first `##` still fails — skipping the whole preamble had reintroduced the F-28 class there.
  - **The escrow never passes over nothing** (ADV-R9-06): an explicit `--run` naming a missing or judge-less run FAILS; a named run is walked through its verify reports even without a RUN-REPORT (the run-died-after-verify case F-69 itself described); the unnamed walk keeps F-36's shield and prints what it skipped.
  - **One audit-trigger section regex, both consumers, any heading level** (ADV-R9-07): `### audit triggers` used to derive a hit in status.mjs while the ##-anchored escrow stayed silent — the exact split formats.json exists to prevent, one key over from the row pattern F-74 had just unified.
  - **The F-72 boundary asks for a real table row** (ADV-R9-08): a wrapped backticked shell pipeline stays a mention — begins-with-a-pipe alone turned a mention into a claim.
  - **GATE-11 reads the config the commit will carry** (ADV-R9-09): index copy, worktree fallback only for an untracked config — an uncommitted `legacy_freeze_exclude` edit disarmed the freeze for one commit with zero trace; an active exclude list is echoed on PASS.
  - **Every G4 refusal names the --spec route** (ADV-R9-10) and GATES-SPEC states the tokenless-legacy vs v(N+1) precedence (DEV-R9-07). Credential tokens are letter-bounded — `${PWD:-…}` is a working directory, `${DB_PASSWORD:-…}` still refused (ADV-R9-11). `report_row` allows CommonMark's 3-space indent and the rows-seen count excludes headers (ADV-R9-12).
  - **A plain install cannot eat repo-local CLAUDE.md content** (DEV-R9-01, blocker): the written section carries a content stamp; a drifted section is REFUSED with the would-be-lost lines shown, `--force-section` is the deliberate escape, and --check's epilogue no longer tells the reader to run the thing that deletes their lines.
  - **Adoption stops dead-ending** (DEV-R9-02/03/04/08): install seeds an empty-but-valid docs/GAPS.md and GATE-12's refusal prints the header row to paste; the install epilogue and INSTALL.md name `[WOW:publish]` as the install-commit lane; install re-stamps `wow_version` (the one key it owns) and --check reports a stale stamp; GATES-SPEC gains the installed Config-keys table and the seeded config names its optional keys and the F-61 hierarchy caveat.
  - **GATE-13 diagnoses a pipe as a pipe** (DEV-R9-05): the F-18 cell-count guard runs before any Non-vacuity blame — the same plan got the right answer from GATE-8 and the wrong rule from GATE-13 at the same G2 moment.
  - **Vacuity speaks with one voice** (DEV-R9-10) and to the line a human reads: gate-2 uses the VACUOUS vocabulary and the sweep summary aggregates ('6/6 passed (2 vacuous)'); the zero-run AT diagnostic says 'no runs yet' instead of implying a parse failure (DEV-R9-09); `check-id --base` refusals and the install epilogue name the config remedy on non-main repos (DEV-R9-11); P3's orchestrate steps renumbered sequentially and README's gate count, GATE-13 billing and preflight-scope claims corrected (DEV-R9-13/14; GATE-5's exact scan scope now stated verbatim, DEV-R9-06).
  - Deferred to v0.7.4 with registry rows: OBL-PKG-26 (consumer-facing finding-id salt, DEV-R9-12) and OBL-PKG-27 (install-vacuity.md limbo + runs/.gate-log hygiene, DEV-R9-15/16), folding into OBL-PKG-25's reduction round.
  - **R9b: a fresh-context delta review attacked the R9 fixes themselves** (docs/reviews/REVIEW-v073-adversarial-delta-ADV-R10.md — 11 CONFIRMED, verdict "not sound as-is"; three fixes had failed inside their own defect class) and every required correction landed in the same unpublished round: the escrow's mention masking covers ~~~ fences and indented code with per-FILE fence state (ADV-R10-01/-04 — the forgery re-ran through the adjacent channels the day the ``` one closed, and one unclosed fence had marked every verify report as fenced); gate-11's carve-out binds only from the COMMITTED config, since the index read fell to commit-then-amend in two plain commands (ADV-R10-02); both consumers locate the audit-trigger section on fence-stripped text, closing the regression where a fenced '# comment' ended the section and hid a recorded hit at both consumers at once (ADV-R10-03); the gate-8 preamble exemption is by NAME (plan_schema.preamble_fields), not by key-shaped costume (ADV-R10-05); every credential token is letter-bounded (TOKENIZER/CREDIT/PASSENGER are not credentials, ADV-R10-06); the ev:commit resolver reads the same mention channels and its one structural residual (an unclosed fence hides a dangling sha; the verifier's re-run is the falsifier) is documented rather than denied (ADV-R10-07); a backticked discharge ev: is named a mention in the refusal (ADV-R10-08); an archived run named with --run is refused as ARCHIVED with F-36 cited, not as missing (ADV-R10-09); CRLF conversion is not "local edits" to the section guard (ADV-R10-10); the no-main install note spares unborn-HEAD main repos (ADV-R10-11); the last two raw table splits in status.mjs read through cellsOf (ADV-R10-12).
  - Suite 331→373 assertions; 24 mutations (M33–M56), each killed by exactly its own tests — M35 (the ADV-R9-06 zero-walk refusal) survived its first run and demanded a new test before dying, which is the proof working.

## v0.7.2-draft — 2026-09-17
- **Pilot #2 batch F-47…platform/F-64 + F-08/F-14 addenda (20 records, from two real runs on v0.6.1).** Seven target code fixed releases ago — F-48/F-61→v0.6.3(F-17), F-49→v0.6.3(F-27), F-52/F-62 core→v0.6.4(F-35), F-57→v0.6.2(braces, both cited shapes verified passing today), F-59→v0.7.1(round 6) — the THIRD consecutive dead-code round. PO decisions 2026-09-17: AT-1 redefined to exclude non-vacuity controls; GATE-12 gains a remediation kind; the branch-model slate adopted in full; and **intake is now coupled to upgrading — the pilots move to v0.7.2 and answer the standing upgrade-RCA question before the next feedback batch is triaged**. Verification honesty: fixes proven against fixtures and engine-side reproductions; pilot repos not reached from this session.
- **The branch model delivers what the plan declares** (F-58, severity-high, two observed instances — instance fix + class closure, parser/model family): unit branches are cut from `int` at the previous wave's close (wave 1 ≡ base), so a wave-N unit can read a wave-<N artifact — that is what wave ordering is FOR; and the plan schema gains `inputs:` with a GATE-8 lint (every input covered by another unit's `owns:` at a STRICTLY lower wave), catching the same-wave-producer case that survived eight revisions and seven adversarial reviews. The silent failure direction — an executor re-creating consolidated work inside its own ownership, green on every gate — is why this is a lint and not a review note.
- **Merge, never rebase** (platform/F-60 — class closure by removal): P3's rebase rewrote every `ev:commit` sha a unit's report cites into dangling objects, invisibly; `git merge --no-ff` preserves them and the linear history bought nothing load-bearing. The residual class detector (resolve every cited sha at sweep time) is OBL-PKG-23.
- **Archived runs leave no refs** (platform/F-63 — instance fix + gate): P5 deletes the run's branches local AND remote after the merge is confirmed, and GATE-7 `--p5` refuses publish while a `wow/<run-id>/*` ref survives for an archived run — 25 dead refs had accumulated in one pilot, each a second, more discoverable home for content the archive owns.
- **The registry stays a registry of what is OWED** (F-51 — instance fix, escrow family): a CV discharged inside its own run (`discharged: <date> ev:…` in its block, FORMATS §5) is a closed question the escrow no longer demands a row for; an unevidenced closure stays demanded, loudly. Eight of eighteen records in one pilot run were closed before publish and all eighteen were being escrowed.
- **GATE-12 gains the remediation category** (F-53 — model fix): repairing a regression the previous run shipped discharges no blocker, so the freeze's only route was a PO overrule; `--kind remediation --ref <defect-record>` is admitted when the ref resolves to a recorded PARK/VF/DEF-plan/CV/registry row (mentions do not count), and new capability under the flag stays a G1 review refusal.
- **AT-1 counts drift, not discipline** (F-47 — semantic fix): the metric excludes non-vacuity controls — negative tests, mutants, planted violations are what GATE-13 demands, and the counter fired hardest on the healthiest runs (two firings, zero audits, both waived — the inert-control class in our own trigger).
- **Parsers stop refusing correct input** (F-56 + F-14 addendum + F-55, the OBL-PKG-20 family — two instance fixes through the shared discipline plus one consumer wiring): GATE-10's row-parse is scoped to the classification table with state reset (a second table's header was reported as a divergence named 'Item'; an `ev:cmd` with regex pipes read as a row; the author's route past both was WORSE evidence); GATE-3 fails a declared status section whose table resolves no Status/Grade column (rows graded by nothing are subject-absent one level down); `ids.obligation` widened to `{2,}` (a pilot is at OBL-*-134) AND wired into `_gap_rows` so it can never again be wrong and inert at once — with GATES-SPEC's ids enumeration replaced by the key set itself (the restated list was eight keys short, scoping the no-inline rule too narrowly).
- **The deriver honors subject-absent** (platform/F-62 residual — class rule applied to status.mjs): a result section the pattern cannot READ is reported `UNREADABLE`, never empty-and-stale — the collapse turned a dialect miss into a 62-record deletion list a PO had approved. The dialect-verdict corpus test (meaning-level parity, not spelling-level) is OBL-PKG-24.
- **Process wording that closes improvisation routes**: quick-lane criterion counts files the change CHOOSES plus gate-forced co-edits, with the mechanical test stated (platform/F-64); the bare `[T:<run-id>]` trailer covers ORCH run bookkeeping at any phase, not just P1/P4 (F-08 addendum — a pilot faked a publish trailer because the wording read narrower than the mechanism); amendments bind when COMMITTED, and wave dispatch resolves every cited amendment id against the unit's branch point (F-54); FORMATS §5 documents the in-run CV closure form.
- Suite 285→305 assertions; 7 mutations (M14–M20), each killed by exactly its own tests.

## v0.7.1-draft — 2026-09-13
- **The audit becomes an institution (engine round 6b, PO decision 2026-09-13).** The trend audit is registered as a RECURRING obligation (OBL-PKG-22: monthly, next due 2026-10-13 — report filed in docs/reviews/, family table walked, row date advanced; a scheduled task on the PO's account fires it, the row is the durable home). CONTRIBUTING gains a **Release quality** section: the five recurring defect families as per-release checks, plus two rules the audit earned — every fix's CHANGELOG entry states instance-vs-class and names the family (class closure: F-18/F-44 were one defect fixed twice because nobody asked), and every consumer-facing recommendation ships with its RCA or a labeled INFERENCE plus the data that would settle it, with a verification-honesty line per release. **OBL-PKG-21 discharged**: parity now fails an open registry row whose successor names a shipped version (mention-rule honored — backticked history is not a claim; the check caught its own first run: two freshly-refreshed cells still cited the old value unbackticked). **A6's mechanical half**: status.mjs prints the installed engine version and the upgrade-check command at the top of every status — pilots ran v0.6.1 for four releases with nothing in their loop showing it; A6's question half goes to both pilots as upstream question docs. Fixes proven against fixtures + reproductions (verification-honesty line; pilot repos not reachable this session).
- **Pre-push self-audit + remediation (engine round 6).** Before this version ever left the repo, a PO-ordered audit of v0.6.0→v0.7.1 trends (docs/reviews/AUDIT-defect-trends-v060-v071.md) found four defects in the round as first staged and three structural patterns behind five releases of findings — and the PO chose full remediation over shipping. Landed: **(a)** the run-id anatomy is named parts (`ids.run_date`/`run_slug`/`run_iter` → `run_core`) and the diagnostics DERIVE from them — GATE-1's zero-hit hints and check-id had restated the grammar as inline literals, in the very release whose headline was "one definition"; the slug cap is now probed from `run_slug`, written nowhere else (mutation proof: shrinking the single source moved every message with zero code edits). **(b)** OBL-PKG-20 opened and half-landed: shared `_table_scan` discipline; the escrow's DEFERRED walk reads status **by column header** (it was positional `cells[1]` — status-by-header was OBL-PKG-13's own discharge rule, never applied to the escrow's walk); plan task rows are admitted by **table membership**, so a bare or mangled id beside valid siblings is an invalid task, loudly — under admission-by-grammar it was invisible and its verify unread (the audit's sharpest fact: F-18 and F-44 were the same defect fixed twice, two rounds apart, because every parser is hand-rolled). **(c)** `jira.scope` marked what it is — convention, ORCH-consumed, not gated (no engine code reads it; the doc previously implied enforcement with no covering obligation). **(d)** F-43's overclaim corrected: a paste raises fabrication cost and gives the verifier a diffable artifact — it is not fabrication-proof; two pilot-relayed claims re-labeled as pilot-reported; cap 48 stated as PO-chosen headroom, not derived. **(e)** OBL-PKG-21 opened (registry self-staleness: four open rows named a successor four versions past) and the aged successor cells refreshed honestly. **Verification honesty:** this round's fixes were proven against engine-side reproductions and fixtures, not against the pilot repos (not reachable this session) — unlike v0.7.0's discharge, which ran against frisbii's real RUN-REPORT.
- **Both-pilot round on v0.7.0-draft: platform F-38…F-46 plus F-13/F-18 addenda; frisbii's f17/f18 re-submissions confirmed already answered by v0.6.4.** Meta-finding first: every new finding was filed against **v0.6.1** — the pilots never upgraded, and three submissions (F-38, the F-13 addendum, F-44's escape half) were fixed before they were written. Pilot upgrades to v0.7.x are the program's highest-value action, and the -draft drop clock does not start until a pilot cycle actually runs this line. PO decisions 2026-09-13: F-43 doc-now + engine half registered (OBL-PKG-19); F-46/F-39 as single-source + cap 48 + early check.
- **The run id has ONE definition** (F-46 + F-39): `ids.run_core` in formats.json is a template every derived shape (task ids, CV ids, branch names, both `[T:]` trailer kinds) expands from at load — in both engines — so the slug cap can never again be restated inconsistently (it was spelled four times; the pilot hit a restatement as "trailer missing" five gates after the cause). The cap rises to **48** (a real brownfield slug broke 24), task ids admit an **optional letter suffix** (`T07a` — a mid-run split is real work and its id must be committable), and `gates.sh check-id <run-id>` refuses an inexpressible id at run open, before anything is signed against it. GATE-1 diagnoses a zero-hit message that carries a trailer-shaped token — naming the token and the actual cause (slug over cap, inexpressible task tail) instead of "missing lane ref"; GATE-8 refuses a plan task id the trailer grammar cannot express, at the moment it is cheap.
- **A gap row with too MANY cells is refused naming the pipe** (F-44): an unescaped `|` inside a cell silently truncated the row under zip() — a registry that looked well-formed lost its discharge column. The parser now demands exact cell count both ways and names the `\|` escape (the write-side rule landed in v0.6.4; the pilot ran v0.6.1, where the truncation was still silent — this closes the read side).
- **Map freshness is content-based** (F-45): fresh ⇔ `git diff --quiet <verified_against> HEAD -- <paths>` — merge ripples, revert pairs and formatting-only chains no longer flag maps stale when nothing under the paths changed; commits that touch paths but leave the tree identical are reported informationally. A gate that cries stale gets its P0 skipped — the boy-who-cried-wolf clause GATE-3's synonym rule already states, applied to GATE-6.
- **Verdict-comparison tables are sayable** (F-41): a fully-backticked cell outside a Status/Grade column is a mention — "executor wrote `COMPLETED`, verifier graded `FAIL`" is the run's most important comparison and was unwritable in any scanned doc. Status/State/Result and Grade/Verdict columns keep claims-by-position: a backticked status can never green a real row.
- **Citations prove form, the verifier's re-run proves truth** (F-43): stated in FORMATS §3 — executors **paste actual command output** (fenced) under each `ev:cmd`; a report without pastes is the verifier's cue to re-run everything cited, and an unreproducible citation is a `VF-` finding about the citation itself. The mechanical half (sampled re-execution / output digests) is OBL-PKG-19.
- **AT-1 counts what survives the run** (F-40): `mocks_fixtures_committed_this_run` — a TDD unit legitimately creates and retires fixtures mid-run; the audit debt is what the repo carries forward (`git diff --diff-filter=A` at the run boundary, not filesystem events).
- **Shared Jira projects declare their slice** (F-42): `jira.scope` in wow.config.json — a JQL fragment the ORCH ANDs into every mapping query, so GATE-10's divergence diff stops drowning in tickets the repo never owned (convention, ORCH-consumed, not gated — FORMATS §10).
- **The Bash-deny comment stops overclaiming** (F-18 addendum): a `Bash()` glob matches the command string as spelled — `git -C . push` walks past a `git push` deny; the template now says the real push controls are the P3 contract and the hooks, and the globs catch the careless spelling, not the determined one.

## v0.7.0-draft — 2026-09-03
- **The release round: the blocking registry is EMPTY for the first time.** PO decisions 2026-09-03: no per-consumer migration paths in a shared open-source package (OBL-PKG-14 closed by decision, policy stated in INSTALL.md — the package guarantees mismatches are seen loudly; reconciling local artifacts is the consuming repo's deliberate work, and the install block on frisbii lifts); OBL-PKG-13 discharged in the engine; the remaining 10 rows are all advisory. Release bar for dropping -draft: one full pilot cycle on this version with no new high-severity findings.
- **OBL-PKG-13 discharged, proven against the real artifact.** The escrow now PARSES registry rows instead of substring-matching raw text (a CV mentioned in another row's prose no longer counts as escrowed); the run-local `CV-<nn>` shorthand is legal inside its own run and resolves to the full id the registry demands (FORMATS §5, `ids.cannot_validate_short`); a DEFERRED task needs an open row REFERENCING it; and F8's third class lands — an audit-trigger hit recorded in a RUN-REPORT needs an open row naming its `AT-n`, or the owed audit retires with the archived run. Against frisbii's actual `260816-wow-gates-r1` RUN-REPORT: the old regex matched **0** CV ids; the new escrow reports **8** shorthand findings, and gate-3 grades its status cells. Status-by-header itself had landed across v0.6.2–v0.6.4 (verdict columns, ungradeable cells, the unread `report_row` regex deleted).
- Versioning note: 0.7 signals the first version whose known debts are all advisory — not API stability; formats may still move between drafts pre-1.0, and FR-1 loudness is the upgrade contract.

## v0.6.4-draft — 2026-09-03
- **Both-pilot round: 11 new findings** (platform F-32…F-37; frisbii: the PII composition, its F-17/F-18, the installer consult, and the S-6 phantom-run addendum). PO decisions 2026-09-03: GATE-2 declarations with scan fallback; PII as GATE-14 + docs; cascade form KEPT (F-32's blocking regex died with v0.6.3's F-14 fix — verified live and tested; only the bare-ids doc note taken).
- **GATE-14 — personal data in staged run evidence** (frisbii): capture-whole + a legitimately widened scope committed 159 third-party customer records with every gate green; the remediation bill was a 381-commit history rewrite. Staged files under `runs/*/evidence/` are scanned for populated personal-data field names; `pii-ok: <reason>` is the visible escape. Plus the P1 widened-scope question and P3's declared-trim capture convention. Wired into the pre-commit hook.
- **GATE-2's obligation set comes from declarations** (frisbii F-17): spec-header `requirements:` and unit `governs:` lists ARE the set when present — a range enrolled two of five rows, and a scanned doc that DISCUSSED an id adopted it, making a repo's own defect record unwritable. Legacy scan stays as fallback with an advisory nudge. Also F-36: an explicitly named run resolving to no plan (active or archived) reports UNGRADED red, and archived runs resolve their plan from the archive — archiving no longer launders a failing gate into "nothing to check".
- **GATE-7 fixed on three fronts** (F-36 + S-6 addendum): the escrow walk is scoped to `--run` (it was failing a publish on another mid-flight run's CV ids); the archive exemption compared a relpath to a TEMPLATE and never fired — real paths now; phantom detection asks git three questions (tracked ⇒ run, untracked-unignored ⇒ mid-creation, only-ignored/empty ⇒ leftover) so a stray .DS_Store neither hides nor revives a phantom, and a destructive instruction never depends on ignore configuration. At `--p5` it also reconciles the feedback log against upstream write-ups (F-37 — the drift happened twice, the first occurrence predicting the second; runs only where a log exists).
- **The machine home is dialect-checked** (F-35): a shared regex used Python's end-of-string escape, which JavaScript reads as a literal 'Z' — gates.py and status.mjs disagreed whether the same repo held 0 or 16 stale quick stubs, and sixteen healthy files were proposed to a PO for deletion. Both keys fixed portably; the parity sweep now refuses the non-portable escapes anywhere in formats.json (it caught its own documentation spelling them, which is the point).
- **Audit triggers derive or say why not** (F-33/F-34): AT-1/AT-2 are read from the run's `## audit triggers` RUN-REPORT table (derived_by 'orch' means read the durable home, never give up — PF-03's founding gap recurring in the file that fixed it); AT-3 is scoped to the run as its own metric name promises (the cross-run sum was permanently HIT, and archiving a run changed the number); AT-5 says 'awaiting PO at G4'.
- **Pipes are writable** (frisbii F-18): `_cells` honors `\|` escapes in every table cell — retiring FORMATS §12's trailing-cell-only caveat — and GATE-8 reports a row/header column-count mismatch with the real cause named instead of blaming the non-vacuity prose.
- **Installer consult guards the schema key** (frisbii): a pre-0.6 `--source` produced a KeyError wearing a registry error's message ('fix the registry' about a file never opened); it now falls through to the completeness check, which refuses old sources in its own honest words.
- FORMATS notes: bare reference ids in report rows (F-32's second trap); §13 states when a write-up is owed.

## v0.6.3-draft — 2026-08-24
- **Pilot #2 (cloakid-platform) backfill round** — 27 live findings from three real runs (F-05…F-31; F-01..04 were v0.6.1's round). Four were already answered by v0.6.2 (F-19/F-23 → balanced braces; F-20 → verdict vocabulary; F-26 → GATE-13 — two pilots independently converged on the plan-verify gap). The rest land here. PO decisions 2026-08-24: bare `[T:<run-id>]` for phase artifacts; GATE-2 two-form split; `ev:attest` + loud unknown kinds; process set as docs + cheap hooks.
- **The empty-is-permissive family closed at every reported instance:** F-12 front-matter block lists parse, an empty `paths` or unresolvable `verified_against` fails loudly (the gate was reporting a FAILED git invocation as fresh); F-13 a plan with units and zero parseable tasks fails GATE-8 naming the id form (29 verify commands were never read); GATE-13 gained the same guard; F-14 an uppercase status token with trailing prose fails instead of falling through (`COMPLETED — verdict NO` was invisible); F-17 CV ids gained the unit segment that IS the allocator (legacy form stays valid).
- **Lane coverage completed (F-07/F-08/F-10):** bare `[T:<run-id>]` resolves to the run directory and carries P1/P4 phase artifacts — the artifact a PO signs is in git at the moment of signing; trailer mentions in backticks/fences no longer count (the mention-vs-claim rule, applied to GATE-1).
- **GATE-2 split into two forms (F-09,** mirroring GATE-9): sweep = consistency lint; `--close` (G4/P4, and the P5 sweep) = the updated-row requirement. The gate previously had no phase where it was both in scope and satisfiable before P4.
- **GATE-9 hardened (F-15/F-29):** with `--run` the governing spec resolves through the run's plan and the gate refuses to guess (the sorted-glob fallback graded whichever spec sorts last); the record may carry its gate (`signed: G4 …`) and a token must match; a signed artifact modified after the commit that introduced its record must carry an `AM-<nn>` amendment record.
- **Evidence grammar (F-27):** `ev:attest{who: claim @date}` for human decisions; an `ev:` token of unknown kind is a loud GATE-3 failure instead of silently not-a-citation. F-05: the inline-code mask is document-wide (spans wrapping a line break leaked). F-19 residual: a missing-citation message now notes when an ev: token IS present on the row (a mangled cell, not a missing citation).
- **Permissions policy (F-06/F-18/F-24):** now a SEED file — copied once, repo-owned, never drift-compared or overwritten; template denies select credential material by location and type (`secrets/**`, key extensions) instead of `*secret*` name-matching that blocked the incident runbooks; writes denied alongside reads; the deny comment claims transcript hygiene, not containment. P5 step 5 is a reviewed merge in the playbook too, matching INSTALL.
- **Process set (F-11/F-16/F-21/F-22/F-25/F-28/F-30/F-31):** TRACEABILITY.md dropped from Load lines (RUN-REPORT + reports/ is the traceability home); mid-run amendments merge base→units and get `AM-<nn>` records; the fix-forward counter counts failures of a fix, with PO classification when the ORCH authored it; a plan-amendment-as-fix gets a delta verifier aimed at the amendment itself; wave-boundary reconciliation walk on both sides of the git/Jira split (engine walker registered as OBL-PKG-18); plan-level `## Contracts` block reaches every executor manifest, and GATE-8 rejects normative language outside it; verifier modes re-derive|audit with plan-marked `audit-only:`; findings indexes point rather than claim (FORMATS §4).

## v0.6.2-draft — 2026-08-24
- **Pilot #1 (frisbii) feedback round** — 11 upstream files reviewed; 3 already answered (probe route v0.5.3, gate-6 vacuity v0.5.3, v0.5.2 parity v0.6.0), F-3 (install omits GATES-SPEC) confirmed fixed since v0.6.0, and the 7 live findings all adopted. PO decisions 2026-08-24: F-9 full (column + lint), F-11 full (step 0 + refusal + gate), HANDOFF limit stays advisory with the count fixed.
- **GATE-13 — plan Verify non-vacuity (F-9).** The pilot proved the class: a task's Verify is the sole mechanical arbiter of COMPLETED, GATE-4's scope predicate cannot see a markdown task row, and nine inert verifies shipped in one run (three past adversarial review). Adopted both halves as designed there: a mandatory `Non-vacuity` cell per task (names the wrong answer the Verify rejects; cites something runnable per GATE-4's rule; `MANUAL` exempts) plus a lint for five idioms that each shipped a real vacuous check, with a visible `lint-ok:` escape. Binds on unsigned plans only (frozen artifacts stay frozen); invoked at G2 with GATE-8, deliberately NOT in the sweep (a mid-P1 run without a PLAN.md is legitimate; at G2 it is not). The pilot's local id was GATE-12 — renumbered 13, 12 was already the obligation block.
- **GATE-3 verdict vocabulary (F-10).** P3 mandates verifier grades and GATE-3 rejected them (56 hits, 10 the mandated `PASS`). A Grade/Verdict column is now checked against `verdict_vocab` — PASS / PASS-with-carry-forwards (CV id in row) / FAIL (VF id in row) — because a status describes the work and a verdict judges it, and COMPLETED+FAIL is the run's most important pair.
- **Citation bodies admit one level of balanced braces** (frisbii braces finding): `awk '{…}'`, jq objects, `{7,40}` quantifiers are citable. An ev: opener the format still cannot parse is reported as *the format cannot express this* — never as the writer's citation being malformed for the wrong reason.
- **Dep probes are allowlisted before execution (S-2).** `probe:` ran under `shell=True` with nothing constraining it — the pilot's demonstration probe wrote a marker file and GATE-6 passed. The first word must now match `probe_allowed` (default: repo-local script; `probe_command_pattern` override in wow.config.json), checked BEFORE running, no calendar fallback. The test with teeth is theirs: the marker file must not exist.
- **Phantom runs refused (S-6).** Archiving strands empty run-id dirs (git tracks no empty directory); status derivation listed them as active for exactly the person who published. gate-7 refuses a run dir holding no files; status.mjs skips it. Predicate is files-on-disk, not git-tracked — deviating from the suggestion deliberately, so a brand-new run before its first commit stays real.
- **P5 step 0 owns the merge to main (F-11)**, and the destructive permissions regeneration (step 5) is guarded: gate-7 at `--p5` refuses a branch behind main (`main_candidates` / `main_branch` override) — the pilot showed a published grant that two blocked waves earned would have been silently dropped.
- **HANDOFF line count fixed** — the counter included the trailing newline, so "80" meant 79 and five consecutive commits trimmed a file that was already right. Counts as `wc -l` now; the limit itself stays advisory (PO 2026-08-24), stated in FORMATS §8.

## v0.6.1-draft — 2026-08-19
- **Pilot #2 round on v0.6.0** (`cloakid-platform`, verified at 80f650d; findings PF-a..PF-d, all four adopted). All fixes ship doc+engine+tests together — no new obligations opened, none needed.
- **PF-d (highest severity): GATE-2 was permanently vacuous for any repo whose requirement ids are not `REQ-nnn`.** The id pattern is now repo-configurable: `requirement_id` in `wow.config.json` (repo-local truth, never overwritten), defaulting to `ids.requirement`; `requirements_row_schema.row` is a template deriving from the effective pattern, so discovery and row-parsing can never disagree; **status.mjs honors the same override** (dual consumer). And FR-1's rule now applies to gate-2 itself: id-shaped REQUIREMENTS rows the effective pattern cannot read fail LOUDLY with a pointer at `requirement_id`, never parse as an empty-and-permissive named set. A brownfield repo (platform's 33 `PLAT-M3-nn` ids) no longer chooses between renumbering stable identities and a gate that lies green.
- **PF-a: GATE-3 no longer reads inline code as claims.** A citation template inside backticks (a registry documenting its own conventions) was flagged malformed — red before any work existed, on the pilot the GAPS-scan decision was made for. Backticked spans are now masked from the evidence scan, fail-safe both ways: a backticked template is not flagged, and a backticked citation cannot satisfy a status.
- **PF-b: parity gained its reverse direction for lane refs.** `check_parity` only ever asked "does the engine have what the docs declare?" — `[WOW:migrate]` sat in formats.json/engine/tests/INSTALL and was absent from LANES.md and the router (where `[WOW:publish]` had also been undocumented since v0.4). Every `commit_trailers` kind now carries a `doc_literal` that must appear in `parity.lane_docs` (LANES.md) and the resident router (`parity.router_locations`), or parity fails. LANES.md gains a Lane refs section defining all five trailers; the router line now enumerates all five. Full named-key coverage both directions remains OBL-PKG-17.
- **PF-c: the single-table-by-construction rule of `docs/GAPS.md` is now stated** (FORMATS §12) — row discovery matches any id-shaped first cell anywhere in the file (deliberate, FR-1), so a second table with id-shaped first cells is read as malformed gap rows; backticked ids are mentions and skipped. The cell-count failure message now says this and names the fix (sibling document).

## v0.6.0-draft — 2026-08-18
- **The engine session.** Doc/engine parity restored: registry worked down from 12 open rows to 7 (all advisory). Discharged: OBL-PKG-07 (G-11 `_governing_sources` gate_2 scope repair merged upstream — step zero), OBL-PKG-02 (gate-6 refuses missing/unparseable plans; subject-absent tests + positive control, non-vacuity proven by disabling the fix), OBL-PKG-01 (gate_12 obligation block with closed-enum validation and audit/fix/probe exemptions; GATE-7 obligation escrow; status.mjs reads GAPS.md and reports what the state requires next; `gap_row` schema), OBL-PKG-08 (layer-parity check runs first in every sweep: spec table vs engine registry vs formats.json, DESIGNED-NOT-IMPLEMENTED markers must name open obligations, version authority incl. the D2 git-history header predicate), OBL-PKG-11 (install.sh refuses on an open scope-matched `blocks-install` row, exit 4).
- TF-01: the test harness's own `| grep -q` under pipefail was a SIGPIPE race — passed on one machine, failed deterministically on another; all pipelines now read to EOF.
- **Two-reviewer framework review** (frisbii + platform standpoints) folded in: FR-1 — an id-shaped registry row the schema cannot read now fails LOUDLY instead of parsing as an empty (permissive) registry (frisbii's legacy 5-column table had made gate-12 green-because-empty); PR-1 — install.sh verifies package self-integrity before writing (missing playbooks/section source/engine files ⇒ exit 5; --check reports MISSING-IN-PACKAGE); PR-4 — `[WOW:migrate]` lane ref, legal only mid-migration (`.planning/` present, freeze unflipped); FR-5 — the blocks-install consult fails CLOSED on registry errors and reports would-refuse under --check; FR-4 — gate-12 wired into P1 step 0; FR-8 — parity says when its version half is skipped; G-15 wording fixed (GATE-3 is sweep-checked, not commit-enforced) discharging OBL-PKG-06; migration playbook: pre-freeze window named, vacuity artifact hand-written until OBL-PKG-09, per-artifact mapping demanded (PR-6/-3/-8); FORMATS §12 pipe-escaping note (PR-7).
- **Independent verification round** (docs/reviews/): grade PASS-with-carry-forwards; fixes landed — F1 install refusal now fires BEFORE any write (test asserts the refused target is untouched); F3 the blocks-install consult fails CLOSED on unreadable/malformed/absent registries (3 wiring tests + advisory control); F2 the G-11 scope fix gained its missing tests (mutation-proven); F4 parity's named-schema half implemented; F5 parity's own inline literals moved into formats.json and a live stale stamp fixed; F6 [WOW:migrate]'s three-state window tested; F7 the subject-absent sentence corrected (an empty table IS a valid registry); F9 unreadable tables of any shape and prose registries now refuse. F8 folded into OBL-PKG-13; OBL-PKG-17 registered (literal-scan rule enforcement).
- PO decisions (2026-08-18, decision memo): effect transitions witnessed in-row + GAPS.md added to scan_targets (transition check lands with engine round 3); pilot-feedback log canonicalized as FORMATS §13; FINDINGS.md adopted as the named audit-run artifact; platform registry slate: OBL-PLAT-01/03/05/11/19 stay blocking, -07/-14 downgraded to advisory with witnessed transitions.
- Registered from review: OBL-PKG-13 (report parsing by column header + CV id policy — blocking), OBL-PKG-14 (upgrade reconciliation; blocks-install scoped to frisbii until it exists), OBL-PKG-15 (G-12/13/14/16 triage), OBL-PKG-16 (feedback-log/FINDINGS/effect-change governance — PO).
- Still open (advisory unless noted): OBL-PKG-03, -04/-05, -09, -10, -12, -15, -16; blocking: -13; scoped blocking: -14.

## v0.5.6 — 2026-08-17
- Pilot round-3 findings: D1 — `blocks-install` consumer clause marked DESIGNED-NOT-IMPLEMENTED and registered as OBL-PKG-11 (v0.5.5 had repeated the layer-parity violation inside the release marking the parity rule; OBL-PKG-07 read as protected while nothing consumed it); scoping decided: install.sh reads the package's own registry, rows carry `scope:` (OBL-PKG-07 = `*` — canonical gate_2 is defective for every target until G-11 merges). D2 — version-stamp predicate corrected to header == version of last-modifying commit (the prior wording passed every stale stamp); stale headers corrected (README, INSTALL, GATES-SPEC, FORMATS). D3 — README status now points at the CHANGELOG authority instead of embedding a version that goes stale. Install modes added (PO design question): `--mode fresh|upgrade|migrate` as declared intent verified against derived target state, refusing on mismatch; drift-refusal guard (never silently overwrite locally-modified files) generalizes the G-11 protection — OBL-PKG-12.

## v0.5.5 — 2026-08-17
- Pilot #2 reassessment residuals + N-findings: parity claim in GATES-SPEC now carries its own DESIGNED-NOT-IMPLEMENTED marker (OBL-PKG-08); install vacuity check is a committed artifact (`docs/install-vacuity.md`), not a mental note; version authority defined (this file's top entry; enforced via parity sweep); `effect` vocabulary closed and widened to `blocks-install` (consumer: install.sh; unrecognized values fail loudly) — OBL-PKG-07 reclassified accordingly; migration steps cited by name, not number.

## v0.5.4 — 2026-08-17 (entry added retroactively in v0.5.5 — its omission was pilot finding N1)
- Pilot #2 (brownfield) plan-review adoptions: OBL-PKG-08 (layer-parity sweep unregistered — the rule violated itself), OBL-PKG-09 (install vacuity report), OBL-PKG-10 (--uninstall); migration gains legacy-invariant re-homing before the GATE-11 freeze (hard stop), permissions regeneration as reviewed merge, brownfield vacuity warning, rollback recipe.

## v0.5.3 — 2026-08-16
- **Subject-absent rule** (from pilot feedback PF-04): every gate now requires two negative tests — violation-present and subject-absent; an absent or unparseable subject is never a pass. Origin: GATE-6 returned PASS for a run with no PLAN.md.
- **Layer-parity rule** (PF-05): spec-first changes are legal only with a paired open obligation in `docs/GAPS.md`; the sweep compares doc-declared gates/schemas against the engine registry.
- GATE-12 and the GATE-7 escrow clause explicitly marked **designed-not-implemented** pending OBL-PKG-01.
- `docs/GAPS.md` added: this repo now uses its own obligation registry (OBL-PKG-01…07).
- Known engine debt (open, blocking new feature work on this package): GATE-12, GATE-7 escrow, status.mjs GAPS derivation, `gap_row` schema, gate-6 subject-absent fix.

## v0.5.2 — 2026-08-16 (spec-only; engine debt tracked as OBL-PKG-01)
- **Obligations get a home** (PF-03): FORMATS §12 — `docs/GAPS.md` is the obligation registry (gap records + `owner` + `effect: advisory | blocks-new-feature-work` + successor + discharge). No new artifact class, no narrative state.
- GATE-12 (obligation block at `/wow-spec`) and GATE-7 obligation escrow **specified** (implementation pending — see v0.5.3 markings).
- P4: audit-trigger hits recorded as durable obligations, not report lines. LANES rule 5: recording findings between runs is quick-lane work.

## v0.5.1 — 2026-08-14
- **Probe-spec route** (PF-02): P1 gains "when the spec cannot be written yet" — decision/fact classification (judgment, not a gate), `SPEC-<feature>-probe-v1` whose ACs govern answer quality, blocked-draft convention, corrections-not-deviations on reopening, the empty-probe trap named (unanswered count PO-accepted at G4), and the no-probes-for-decisions guardrail.

## v0.5.0-draft — 2026-08-14
- **Runnable enforcement engine**: gates.sh (+gates.py), formats.json, status.mjs, install.sh, permissions-policy template; per-gate negative tests + install/wiring tests (19 passing).

## v0.4.1 — 2026-08-13
- **External-dependency maps** (PF-01): FORMATS §11 — `docs/deps/<name>.md` with probe-hash freshness (max_age fallback); GATE-6 extended to declared deps; P0 probe-refresh step.

## v0.4 — 2026-08-10
- Initial public draft: thin CLAUDE.md router, phase playbooks P0–P5 (audience-labeled), LANES, FORMATS, GATES-SPEC (GATE-1…11), INSTALL with three-layer reliability model and GSD coexistence (GATE-11).
